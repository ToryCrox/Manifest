<?xml version="1.0" encoding="UTF-8"?>
<manifest>

  <!-- remote: name 名称，fetch 远程地址。可以有多个remote，通过project.remote可指定不同的remote -->
  <remote name="origin"
          fetch="git@git.hetaozs.com:mimikkoui_system/"
          review="git@git.hetaozs.com:mimikkoui_system/" />

  <!-- default: revision 指定分支，remote 指定remote name，sync-j 同步线程数 -->
  <default revision="2.0-alpha"
           remote="origin"
           sync-j="4" />

  <!-- project：path 本地相对路径，name 远程相对路径，完整路径为 remote.fetch + project.name -->
  <project path="./" name="MimikkoUI.git" />
  <project path="common/base" name="base.git" />
</manifest>